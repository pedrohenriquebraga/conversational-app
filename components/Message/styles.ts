import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 12,
    maxWidth: 300,
    borderRadius: 8,
    flexDirection: "row",
    marginBottom: 12,
  },
  message_text: {
    fontSize: 16,
  },
});
